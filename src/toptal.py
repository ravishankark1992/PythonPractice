import cv2

def hist_equalization(input_image):

    # h(v) = ((cdf(v) - cdf_min) / (M * N - cdf_min) * 255)

    return
image_path = ""
#input_image = cv2.imread(image_path)
#out = hist_equalization(input_image)
